# Use -Dmobile.participant=mobile.android or -Dmobile.participant=mobile.ios
# to package a test archive for Android or iOS tests.
mobileTest.participant1Prefix=${mobile.participant}

# Appium FIXME figure out with prefix
#appium.server.address=127.0.0.1
#appium.server.port=4723

# Android
mobile.android.type=android
# path to apk file
mobile.android.caps.app=${mobile.app}
mobile.android.caps.deviceName=${mobile.deviceName}

# When running locally you may want to reinstall the app every time to reproduce
# all permissions dialogs (if the permissions have been accepted).
#android.reinstallApp=false

# iOS
mobile.ios.type=ios
mobile.ios.caps.app=${mobile.app}
mobile.ios.caps.deviceName=${mobile.deviceName}

# Check ParticipantBuilder and it's subclasses for more configuration properties
